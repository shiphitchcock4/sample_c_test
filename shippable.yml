language: none

build:
  pre_ci:
    - echo 'true'

  pre_ci_boot:
    image_name: drydock/u12cpp
    image_tag: prod
    pull: false
    options: '--privileged=true --net=bridge -e FOO=true -e BOO=false'
    
  post ci:
    - docker build -t=shippabledocker/shiphitchcock4_sample_cpp .
    - docker tag -f drydock/u12cpp:latest shippabledocker/shiphitchcock4_sample_cpp
    - docker push shippabledocker/shiphitchcock4_sample_cpp


  ci:
    - ./configure && make && make install
    - ./hello

  post_ci:
    - echo "true"
